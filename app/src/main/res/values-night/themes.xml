<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Boxyz" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/night_primaire</item> <!-- premiere couleur -->
        <item name="colorPrimaryVariant">@color/night_primaire_varient</item> <!-- deuxieme couleur -->
        <item name="colorSecondary">@color/night_secondaire</item>
        <item name="colorSecondaryVariant">@color/night_secondaire_varient</item>
        <item name="colorAccent">@color/night_accent</item>
        <item name="colorOnBackground">@color/night_background</item> <!-- background -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnBackground</item>
        <!-- Customize your theme here. -->
    </style>
</resources>